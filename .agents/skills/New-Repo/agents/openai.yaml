interface:
  display_name: "New Repo"
  short_description: "Repo-specific patterns and workflows for New-Repo"
  default_prompt: "Use the New-Repo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true